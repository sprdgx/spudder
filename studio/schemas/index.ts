import blockContent from './blockContent'
import tripSchema from './tripSchema'
import userSchema from './userShema'
import Rides from './Rides'

export const schemaTypes = [userSchema, Rides, tripSchema, blockContent]
